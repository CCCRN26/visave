import{requireAuth}from'@/lib/auth/session';import{requireGroupRouteAction,GROUP_ACTION}from'@/modules/group-access/group-access.service';import GroupNav from'@/components/group-nav';import StartMeetingForm from'@/components/start-meeting-form';export default async function NewMeeting({params}){const{id}=await params,u=await requireAuth();await requireGroupRouteAction(u,id,'meeting.create',GROUP_ACTION.MEETING_OPERATE);const today=new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Lagos',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());return <><h1>Start Meeting</h1><p className="muted">Opening balances and the active-member attendance snapshot are created automatically.</p><GroupNav id={id}/><StartMeetingForm groupId={id} today={today}/></>}
