import Link from'next/link';import{requireAuth}from'@/lib/auth/session';import{requireGroupRouteAction,canGroupAction,GROUP_ACTION}from'@/modules/group-access/group-access.service';import{getMeetings}from'@/modules/meetings/meeting.service';import GroupNav from'@/components/group-nav';import DataTable from'@/components/data-table';import{formatDate,formatDateTime}from'@/lib/utils/date';import{formatCurrency}from'@/lib/utils/money';export default async function Meetings({params}){const{id}=await params,u=await requireAuth(),actor=await requireGroupRouteAction(u,id,'meeting.view',GROUP_ACTION.MEETING_VIEW),canOperate=canGroupAction(u,actor,GROUP_ACTION.MEETING_OPERATE);const rows=await getMeetings(id);return <><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><h1>Meetings</h1><p className="muted">Operational meeting history and derived financial activity.</p></div>{canOperate&&<Link className="button" href={`/groups/${id}/meetings/new`}>Start meeting</Link>}</div><GroupNav id={id}/><DataTable rows={rows} columns={[{key:'meeting_number',label:'Meeting',render:x=><Link href={`/groups/${id}/meetings/${x.id}`}>Meeting {x.meeting_number}</Link>},{key:'meeting_date',label:'Date',render:x=>formatDate(x.meeting_date)},{key:'cycle_number',label:'Cycle'},{key:'attendance_count',label:'Attendance',render:x=>`${x.attended}/${x.attendance_count}`},{key:'savings',label:'Savings',render:x=>formatCurrency(x.savings)},{key:'social_fund',label:'Social Fund',render:x=>formatCurrency(x.social_fund)},{key:'fines',label:'Fines',render:x=>formatCurrency(x.fines)},{key:'status',label:'Status'},{key:'opened_by_name',label:'Opened By'},{key:'closed_at',label:'Closed',render:x=>formatDateTime(x.closed_at)}]}/></>}
