import{requireAuth}from'@/lib/auth/session';import{requireGroupRouteAction,GROUP_ACTION}from'@/modules/group-access/group-access.service';import{query}from'@/lib/db/query';import{notFound}from'next/navigation';import OnboardingWizard from'@/components/onboarding-wizard';export default async function Onboarding({params}){const{id}=await params,u=await requireAuth();await requireGroupRouteAction(u,id,'group.update',GROUP_ACTION.MEMBER_MANAGE);const[g]=await query(`SELECT g.*,p.name project_name,s.name state_name,l.name lga_name,concat(u.first_name,' ',u.last_name) facilitator_name FROM vsla_groups g JOIN projects p ON p.id=g.project_id LEFT JOIN states s ON s.id=g.state_id LEFT JOIN lgas l ON l.id=g.lga_id LEFT JOIN users u ON u.id=g.facilitator_user_id WHERE g.id=$1 AND g.organization_id=$2`,[id,u.organization_id]);if(!g)notFound();return <><h1>Onboard {g.name}</h1><p className="muted">Progress is calculated from persisted group data. Save and return at any time.</p><OnboardingWizard group={g}/></>}
