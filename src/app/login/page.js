"use client";import Image from "next/image";import Link from "next/link";import{useState}from"react";import{useRouter}from"next/navigation";export default function Login(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(event){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget),response=await fetch("/api/v1/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})}),json=await response.json();if(!response.ok){setBusy(false);return setError(json.error?.message||"Login failed")}const me=await fetch("/api/v1/auth/me").then(value=>value.json());setBusy(false);const roles=me.data?.roles||[],memberOnly=roles.includes("VSLA_MEMBER")&&!roles.some(role=>["SUPER_ADMIN","PROJECT_ADMIN","STATE_COORDINATOR","FACILITATOR"].includes(role));router.replace(memberOnly?"/my-groups":"/dashboard");router.refresh()}return <main className="login-page"><section className="login-story"><Link className="public-brand" href="/" aria-label="Visave home"><Image className="public-brand-logo" src="/cccrn-logo.png" alt="CCCRN logo" width={190} height={148}/><span><strong>Visave</strong></span></Link><div><p className="section-kicker">Community finance, made clearer</p><h1>Community savings.<br/>Stronger livelihoods.<br/>Better health.</h1><p>Secure tools for programme teams, Agents and elected group leaders.</p></div><small>Visave is a CCCRN programme.</small></section><section className="login-form-side"><form className="login-card" onSubmit={submit}><p className="eyebrow">Secure platform access</p><h2>Welcome back</h2><p className="muted">Sign in to continue to your workspace.</p><label>Email address<input name="email" type="email" required autoComplete="email" placeholder="you@example.org"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button disabled={busy}>{busy?"Signing in…":"Sign in"}</button><div className="login-links"><Link href="/">Back to website</Link><Link href="/find-a-group">Find a Group</Link></div></form></section></main>}
