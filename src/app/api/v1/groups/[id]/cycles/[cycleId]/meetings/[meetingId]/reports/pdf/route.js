import { requireAuth } from "@/lib/auth/session"; import { fail } from "@/lib/errors/response"; import { buildMeetingReportPdf, meetingReportPdfFilename } from "@/modules/reports/meeting-report-pdf"; import { getMeetingReport } from "@/modules/reports/meeting-report.service";
export async function GET(_request, { params }) { try { const { id, cycleId, meetingId } = await params; const report = await getMeetingReport(await requireAuth(), id, cycleId, meetingId, "export"); const pdf = await buildMeetingReportPdf(report); return new Response(pdf, { headers: { "Content-Type": "application/pdf", "Content-Disposition": `attachment; filename="${meetingReportPdfFilename(report)}"`, "Cache-Control": "private, no-store" } }); } catch (error) { return fail(error); } }
