import { requireAuth } from "@/lib/auth/session"; import { fail } from "@/lib/errors/response"; import { buildCycleReportPdf, cycleReportPdfFilename } from "@/modules/reports/cycle-report-pdf"; import { getCycleReport } from "@/modules/reports/cycle-report.service";
export async function GET(_request, { params }) { try { const { id, cycleId } = await params; const report = await getCycleReport(await requireAuth(), id, cycleId, "export"); const pdf = await buildCycleReportPdf(report); return new Response(pdf, { headers: { "Content-Type": "application/pdf", "Content-Disposition": `attachment; filename="${cycleReportPdfFilename(report)}"`, "Cache-Control": "private, no-store" } }); } catch (error) { return fail(error); } }
