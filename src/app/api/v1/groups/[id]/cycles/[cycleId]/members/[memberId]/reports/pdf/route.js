import { requireAuth } from "@/lib/auth/session"; import { fail } from "@/lib/errors/response"; import { buildMemberStatementPdf, memberStatementPdfFilename } from "@/modules/reports/member-statement-pdf"; import { getMemberStatement } from "@/modules/reports/member-statement.service";
export async function GET(_request, { params }) { try { const { id, cycleId, memberId } = await params; const report = await getMemberStatement(await requireAuth(), id, cycleId, memberId, "export"); const pdf = await buildMemberStatementPdf(report); return new Response(pdf, { headers: { "Content-Type": "application/pdf", "Content-Disposition": `attachment; filename="${memberStatementPdfFilename(report)}"`, "Cache-Control": "private, no-store" } }); } catch (error) { return fail(error); } }
