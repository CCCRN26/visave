import test from "node:test";
import assert from "node:assert/strict";
import fs from "node:fs";
import { createFacilitatorSchema, deactivateFacilitatorSchema } from "../src/modules/facilitators/facilitator.schemas.js";
const id = "aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa";
test("facilitator creation validates login, fixed role scope and optional assignments", () => { const parsed = createFacilitatorSchema.parse({ firstName: "Ada", lastName: "Field", email: "ADA@example.org", phone: "08012345678", staffCode: "fac-nig-001", password: "Secure-Test-2026!", projectId: id, stateId: id, lgaId: id, groupIds: [id] }); assert.equal(parsed.email, "ada@example.org"); assert.equal(parsed.staffCode, "FAC-NIG-001"); assert.equal(Object.hasOwn(parsed, "roleCode"), false); });
test("facilitator password and deactivation replacement are constrained", () => { assert.equal(createFacilitatorSchema.safeParse({ firstName: "A", lastName: "B", email: "a@example.org", staffCode: "FAC-1", password: "short", projectId: id, stateId: id }).success, false); assert.equal(deactivateFacilitatorSchema.safeParse({ replacementFacilitatorId: id }).success, true); });
test("Phase 2E migration grants lifecycle permissions without elevating facilitator role", () => { const sql = fs.readFileSync(new URL("../database/migrations/025_phase_2e_facilitator_permissions.sql", import.meta.url), "utf8"); for (const permission of ["facilitator.create", "facilitator.update", "facilitator.activate", "facilitator.deactivate", "facilitator.assign_groups"]) assert.ok(sql.includes(permission)); assert.doesNotMatch(sql, /r\.code='FACILITATOR'/); });
