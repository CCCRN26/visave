import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';import{joinRequestSchema,contactSchema}from'../src/modules/public/public.schemas.js';import{transitionSchema}from'../src/modules/membership-requests/membership-request.schemas.js';test('public join input is bounded and validated',()=>{const r=joinRequestSchema.parse({firstName:' Ada ',surname:'Okafor',phone:'08012345678',stateId:crypto.randomUUID(),lgaId:crypto.randomUUID()});assert.equal(r.phone,'08012345678');assert.equal(joinRequestSchema.safeParse({...r,message:'x'.repeat(1001)}).success,false)});test('contact requires a contact channel and bounded message',()=>{assert.equal(contactSchema.safeParse({name:'Visitor',category:'OTHER',message:'Please contact me'}).success,false)});test('group decisions require notes',()=>{assert.equal(transitionSchema.safeParse({status:'APPROVED'}).success,false)});test('public migration uses privacy-safe defaults and duplicate protection',()=>{const sql=fs.readFileSync('database/migrations/028_agent_led_onboarding_public_membership.sql','utf8');assert.match(sql,/DEFAULT 'HIDDEN'/);assert.match(sql,/join_requests_active_phone_unique/);assert.match(sql,/created_by_facilitator_id/)});
